spring:
  application:
    name: kitchen
  datasource:
      url: "jdbc:postgresql://localhost:5432/los_pollos_hermanos"
      username: "admin"
      password: "admin123"
  jpa:
      show-sql: true
      properties:
          dialect: "org.hibernate.dialect.PostgreSQLDialect"
rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
  queue:
    name: food_item_work
  exchange:
    name: los_pollos_hermanos
  routing:
    key: los_pollos_hermanos-food_item_work
workforce:
  amount: 6